class Solution:
    def firstMissingPositive(self, nums):
        n = len(nums)

        # Bước 1: đưa về đúng vị trí
        for i in range(n):
            while 1 <= nums[i] <= n and nums[nums[i] - 1] != nums[i]:
                correct_idx = nums[i] - 1
                nums[i], nums[correct_idx] = nums[correct_idx], nums[i]

        # Bước 2: tìm số thiếu
        for i in range(n):
            if nums[i] != i + 1:
                return i + 1

        return n + 1
